# 완료보고서 — v2.13.1 ~ v3.1.0 (녹음 코치 + UX 개편)

- 프로젝트: SingPromfter
- 작업 폴더: `C:\Projects\SingPromfterApp\.claude\worktrees\upbeat-northcutt-5f116e`
- 작성일: 2026-07-30 (직전 체크포인트 08:32 이후 증분)
- 작업자: Claude Code
- 커밋: `dd3b0cc` → `a98bddb` (11커밋, PR #2) + svil-ai-work `2079fb0`

## 요약

하루 안에 11개 릴리스. 큰 덩어리는 셋: **v3.0.0 녹음 코치**(음정·박자 채점 + AI 보정, 새 로컬 서버), **UX 개편**(곡 목록 드래그 재정렬·드로어 재배치·전체 테두리 선택 표시), **단축키 체계 확립**(자판 무관 판정·홈/무대 동작 단일화). 전부 실기 배포·검증.

## 릴리스별 핵심

| 버전 | 내용 |
|---|---|
| v2.13.1 | 단축키가 곡 검색·설정 등 다른 탭에서 먹던 사고 — 탭 게이트(홈·즐겨찾기·무대만) |
| v2.14.0 | 곡 목록을 예약 큐처럼 드래그 재정렬 — '내 순서' 정렬 신설, 필터 중에도 동작, 순서·정렬 모드 저장 |
| v2.14.1 | 선택 곡 표시를 왼쪽 막대 → 네 변 전체 테두리 |
| v2.15.0 | [받아쓰기 AI] — 로컬 STT로 싱크 가사 생성(곡 길이 밖 환청 필터) + 프롬프터 인라인 가사 수정(길게 누르기) |
| v2.16.0 | 예약 큐 상단 고정·전체 높이·드로어 토글(기본 열림, 자동 접힘 제거) + 하단 재생바 드로어화(기본 숨김) |
| v2.17.0 | T=싱크 리셋, `.`=늦춤 `/`=앞당김(방향 교체), E=현재 줄 편집(ESC 저장) |
| v2.17.1 | 일시정지 중 T·./ 미반영(위치 틱 의존) + Shift 잔상으로 `/` 무시 — 즉시 재계산·`>`·`?` 수용 |
| v3.0.0 | **녹음 코치** — 음정·박자 채점 + AI 보정 (아래 상세) |
| v3.0.1 | `.`·`/` 자판 무관(실제 입력 문자) 판정 + 예비 키 `[`·`]` · 맨휠 제거(Alt+휠 간섭 원천 차단)·O/P 줄 이동 · 설정에 단축키 안내 |
| v3.0.2 | 무대=메인 기능 동기화(Space·R·E·싱크 줄) + **PrompterActions 단일화**(두 화면 배선을 한 곳으로) |
| v3.1.0 | 드로어를 숨긴 만큼 가사 창이 실제로 커진다 — 손잡이 두 개 한 줄 배치(<90px), 무대 stableStage 고정 제거(+216px) |

## v3.0.0 녹음 코치 상세

- **새 로컬 서버** `pitch_system`(포트 8773, `C:\Projects\svil-ai-work\pitch_system`, svil-ai-work `2079fb0`):
  - 분석: torchcrepe(신경망 f0, GPU) — 녹음 vs 원곡 보컬 스템, 음표 단위 센트/ms 편차 + 점수. 옥타브 접기, 전조(구운 키+사용자 키) 반영.
  - 보정: parselmouth(Praat PSOLA) 음정 스냅 + 전체 박자 오프셋. **합성 검증: +50센트/+100ms → ±3센트/0ms(점수 56→98)**.
- 앱: 녹음 목록에 [음정 체크](PitchReportDialog — 점수 2 + "01:23 · 음정 반음쯤 낮게 (−78센트) · 박자 0.3초 늦게" 진단 목록)와 [AI 보정](목소리만/반주와 믹싱). 보정본은 'AI 보정본' 배지 새 테이크(correctedFrom 링크) — 기존 재생·삭제·믹싱 그대로.
- STT 서버 확장: `with_segments=1`(타임스탬프)·`vad=0`(노래) — 기존 클라이언트 무영향.
- "오픈AI 엔진"은 OPENAI_API_KEY 부재 + 로컬 우선 원칙으로 **로컬 오픈 AI 엔진**으로 해석(사용자 이의 없음).

## 새 규약 (이번 증분)

- **PrompterActions**(prompter_keyboard_scope.dart): 홈·무대가 소비하는 동작 묶음. 새 단축키/동작 = actions 필드 + 스코프 처리기 한 곳.
- 단축키 최종: Space·F5·ESC·R·T(리셋)·E(편집, ESC 저장)·O/P(줄)·`.[`(늦춤)·`/]`(앞당김, 꾹 누르면 연속)·←→·Home/End·↑↓·Ctrl/Alt/Shift+휠. **맨휠 없음**. 설정 탭에 안내 표.
- 문장부호 단축키는 논리 키+Shift 잔상+**실제 입력 문자** 3겹 판정(한글 자판 OEM 매핑 어긋남 대응).
- 오프셋 변경은 위치 틱을 기다리지 않고 즉시 줄 재계산(applyLyricsOffset).
- 곡 순서 = songs.json 나열 순서가 정본, 정렬 모드는 설정(songSortMode) 저장.
- 드로어 닫힘 = 실공간 반환(무대 stableStage 보정 제거, 홈 손잡이 한 줄).

## 검증

- analyze 0건 · 테스트 **723건**(v2.13.0 대비 +46) · Windows 빌드 · dist 배포 v3.1.0 실기 확인
- PR #2: https://github.com/kuroicode-beep/SingPromfter/pull/2 (30커밋)
- 「너를 사랑하고도」 STT 가사 47줄 부착(환청 2줄 자동 필터), 남자키(MR−5/키조절−7) 구성으로 실등록됨

## 남은 것 / 다음 후보

1. **녹음 코치 실사용 검증** — 실제 녹음으로 [음정 체크]·[AI 보정] 귀 검증 (합성 검증만 완료)
2. 박자 보정 음표별 신축(현재는 전체 오프셋만)
3. 남자키 −5/−7 슬롯 실청, 「선물」·「봄이 와도」 싱크 실청 (이월)
4. PR #2 머지 여부 (30커밋, CI 확인)
5. pitch_system 서버 현재 켜져 있음(8773) — 안 쓰면 종료. 분리 서버는 꺼짐
