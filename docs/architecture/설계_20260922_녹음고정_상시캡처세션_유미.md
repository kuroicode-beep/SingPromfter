# 녹음 고정 — 상시 캡처 세션 설계 (v5.16.0)

작성 2026-09-22 · 유미(Claude Code) · 감사 3갈래 → 설계 3안 → 심사 3렌즈의 종합본

## 1. 왜 바꾸는가

사용자 요청(원문): 「녹음 고정 상태에서 스페이스로 녹음 시작하면 앞에 딜레이가 있어서 시작 시점 잡기가 어렵다. 딜레이 없애달라.」 / 「녹음 및 재생은 바로 시작하고 저장 과정에서 리드인을 넣는 방식으로 바꿔.」

v5.15.2가 없앤 것: ffmpeg 레벨 줄 버퍼링(`direct=1` 누락) 탓에 스페이스마다 돌던 4.5초 입력 점검.

**아직 남은 것**: 스페이스를 누른 뒤 ffmpeg가 dshow 그래프를 짓는 0.44~0.53초 동안은 마이크가 물리적으로 닫혀 있다. 버퍼 크기·프로브 옵션·콜드/웜 어느 것으로도 40ms 넘게 줄지 않았다(실측). 테이크마다 프로세스를 띄우는 구조에서는 이 구간의 목소리를 구할 방법이 없다.

**해법**: 녹음 고정을 켜는 순간 ffmpeg 하나를 띄워 세션 파일에 계속 받아 둔다. 스페이스는 「표시(마크)」만 찍고 곧바로 재생을 건다. 정지 때 세션 파일에서 구간을 잘라 테이크로 저장한다.

## 2. 실측 근거 (감사, RØDE NT-USB Mini, ffmpeg 8.1.1)

| 항목 | 값 |
| --- | --- |
| spawn → 파일 t=0 | 440~531ms (v5.15.2 실기 테스트 474ms) |
| `-audio_buffer_size 50` 프레임 간격 | 50.0ms, 첫 RMS − t0 = 81~100ms |
| `-flush_packets 1` + raw PCM 파일 성장 | 50ms(4800바이트)씩, 실시간 대비 0~70ms 늦음 |
| 쓰는 중인 파일을 다른 핸들로 읽기 | 가능(1.5초 조각 읽어 WAV 쓰기 12ms) |
| 벽시계↔파일시각 기준점(최소값 필터) | dshow start와 1~14ms, 파일 크기 기준점과 7~10ms 일치, 45초 드리프트 0.7ms |
| `IMFMediaEngine::Play()` → 클럭 첫 전진 | 10~23ms (N=24) |
| 'q' → 종료 | 104~140ms |

## 3. 구조

### 3.1 세션 프로세스

```
ffmpeg -hide_banner -f dshow -audio_buffer_size 50 -i audio=<장치>
       -ac 1 -ar 48000 -af <volume,>astats=metadata=1:reset=1,ametadata=print:key=lavfi.astats.Overall.RMS_level:file=-:direct=1
       -nostats -flush_packets 1 -t 2700 -f s16le -y <세션폴더>/<세션id>.pcm
```

- `-progress`는 뺀다(515ms 간격이라 쓸모없다).
- raw s16le mono 48k — 헤더가 없어 바이트 오프셋 = ms × 96.
- `-t 2700`: 앱이 죽어 고아가 돼도 45분·259MB에서 스스로 끝난다. 상한에 닿아 정상 종료(코드 0)하면 고정 중일 때 세션을 다시 연다.
- 세션 폴더: `getApplicationSupportDirectory()/capture_sessions/`. **OneDrive 밖**(자라는 파일이 동기화에 걸린다)이고 **%TEMP% 밖**(이 PC는 05시 정리 크론이 %TEMP%를 쓴다).
- 고정 중에는 2채널(반주 장치)을 열지 않는다 — 보컬 1채널. 설정에 반주 장치가 있으면 한 번 안내한다.

> **구현 확정 (3.1)**
> - 상한에 닿았을 때 **조각이 열려 있으면** 재기동하지 않고 3.6의 끊김 경로로 처리한다(끊긴 데까지 저장 → 고정 해제 → 큰 경고). 조각이 없을 때만 세션을 다시 연다.
> - 그 손실을 줄이려고 **대기 중(조각·저장 대기 없음)에 35분(`kSessionRolloverSeconds`)을 넘기면 미리 새 세션으로 갈아탄다**('q' → 새 세션, 약 1.5초 동안 스페이스는 「마이크를 여는 중」으로 거절). 상한까지 10분이 남으므로 조각 하나가 10분을 넘지 않는 한 도중에 닿지 않는다. 저장 대기 조각이 있으면 미룬다(끝 바이트가 닿기 전에 'q'를 보내면 끝이 잘린다).
> - 세션을 연 앱은 수명 동안 `<세션id>.lock`을 배타 잠금으로 쥔다(3.6의 소유 판정). 자식 ffmpeg는 이 핸들을 물려받지 않는다.

### 3.2 시계 (CaptureClock)

- 단조 `Stopwatch` 하나(µs). 프레임 줄 도착 시각과 스페이스 마크를 같은 시계로 찍는다.
- ametadata는 프레임마다 머리줄(`frame:N pts:… pts_time:T`) + 값 줄을 낸다. 프레임 k의 끝 시각은 **다음 줄의 pts**로 확정한다(프레임 길이를 가정하지 않는다).
- `offset = (앞 줄 도착 벽시계) − (다음 줄 pts)` 의 **슬라이딩 최소값**(1초 버킷 × 10개)이 기준점 `t_ref`. 도착 지연은 늦어지는 쪽으로만 생기므로 최소값이 참값에 가장 가깝다. 전역 최소가 아니라 창인 이유는 USB 마이크 발진기와 QPC의 드리프트를 따라가기 위해서다.
- 파일시각 `F(W) = W − t_ref − gap(W)`. pts 간격이 공칭+20ms를 넘으면 드롭으로 보고 그 차이를 `gap`에 누적한다(raw PCM은 pts 없이 이어 쓰이므로).
- **마크는 벽시계(µs)로만 저장**하고, 파일시각 환산은 저장(슬라이스) 시점의 수렴된 `t_ref`로 한다. gap은 「그 마크 시각까지」만 적용한다.
- 5프레임 이상 표본이 쌓여야 `isLocked`. 잠기기 전에는 마크를 거절한다.

> **구현 확정 (3.2)**
> - 파일시각은 「저장 시점의 기준점 하나」가 아니라 **마크 시각 앞뒤 ±9버킷(±9초) 창의 최소값**(`tRefUsAt`)으로 환산한다. 기준점 하나로 환산하면 테이크 길이 × 드리프트만큼 어긋난다(50ppm × 4분 = 12ms). 그 무렵 버킷이 비어 있으면 가장 가까운 버킷을 쓴다. gap은 그대로 그 마크 시각까지만 적용한다.
> - **세션 첫 간격만 드롭 문턱이 5ms**(`kClockStartGapSlackUs`)다. dshow는 첫 버퍼 뒤에서 pts를 한 번 건너뛰고(실측 RØDE 26~36ms, Razer 17~23ms, FLOW 8 0~4ms) 그만큼이 파일에 없다. 20ms 문턱으로는 Razer의 점프가 구멍으로 안 잡혀 세션 내내 파일시각이 그만큼 뒤로 밀린다. 그 뒤의 간격은 공칭+20ms 그대로다.
> - 마크 환산만 구멍을 알면 조각 **내용**은 어긋난다 — 조각 도중(시작 마크 뒤)의 구멍은 저장할 때 그 자리에 **무음을 끼워**(`insertSilence`) 구멍 뒤의 소리를 제 곡 좌표에 놓는다. 길이도 벽시계 길이와 맞는다. 메웠으면 저장 토스트에 한 줄로 알린다.

### 3.3 스페이스 흐름 (녹음 고정 중)

시작:

1. 세션이 live + clock locked + 입력 점검 통과가 아니면 → 스낵 「마이크를 여는 중입니다 — 잠시만요」, **재생도 시작하지 않는다**(음악만 나오고 녹음은 안 되는 조용한 실패 방지).
2. 같은 동기 스택에서: `P0 = playback.precisePosition`, `markTakeStart(wallUs, P0)`, 곡·슬롯·피치·activeAudioPath·tempo를 불변 컨텍스트로 캡처.
3. `await playback.play()`. 재생이 막히면(메시지 반환) `cancelOpenTake()`.
4. 첫 await 전에 상태(`isTakeOpen`)가 뒤집히므로 재진입·유령 녹음이 구조적으로 없다. 분기 기준은 `playing`(이벤트 지연으로 늦게 서는 값)이 아니라 동기 상태 `isTakeOpen`.

정지:

1. `markTakeEnd()`(동기) → `await playback.pause()` → 저장은 **직렬 큐**(`_armedSaveChain`)에 올리고 기다리지 않는다. 다음 스페이스를 막지 않는다.
2. `pause()`는 `state.playing` 게이트를 탄다(prompter_audio_service.dart:135). 재생 직후 상태 이벤트가 오기 전에 정지하면 무동작이 되므로, 고정 모드 정지는 게이트를 우회하는 `forcePause()`를 쓴다(시작도 `forcePlay()`).
3. `pause()` 끝에서 네이티브 `currentPosition`으로 `_clock` 앵커를 확정한다 — 화살표 seek 없이 곧바로 다음 조각을 걸 때의 P0 오차를 줄인다.

조각이 없는데 재생 중이면 그냥 pause만 한다.

### 3.4 잘라내기 (computeTakeSlice — 순수 함수)

- `leadIn = min(300, F_start)` — 스페이스 300ms 전부터 담는다(「저장 과정에서 리드인」).
- `sliceStart = F_start − leadIn`, `sliceEnd = min(F_end − 60, 파일 길이)`. 끝 60ms는 정지 키 클릭 구간이라 잘라 내고 20ms 페이드아웃.
- 시작 키 클릭 `[F_start − 40, F_start + 60]`는 5ms 램프로 뮤트. 리드인 안쪽이라 이어붙이기에서는 어차피 건너뛰는 구간이다.
- `songPositionMs = max(0, P0 − leadIn − L)`, `L = 15ms`(재생 시작 지연 실측 중앙). 이미 재생 중일 때 찍은 마크(R 키)는 L을 빼지 않는다.
- 불변식: `songPositionMs + leadInMs + L == P0` (클램프 전). 속성 테스트로 고정.
- 마크 구간 < 500ms(`kMinimumTakeDuration`)면 null → 「너무 짧아 저장하지 않았습니다」.
- 끝 바이트가 디스크에 닿을 때까지 **조건 루프**로 기다린다(10ms 간격, 500ms 상한, 넘으면 있는 데까지).
- 테이크 WAV는 `.part`로 쓰고 → 크기 검증 → rename. 실패하면 세션 파일을 지우지 않고 큰 경고(CenterAlert).
- 세션 PCM 원본은 **고정을 끌 때까지 지우지 않는다**(트림 상수가 안 맞을 때의 복구 여지).

> **구현 확정 (3.4)** — 위의 식 두 줄은 아래로 읽는다.
> - `leadIn = min(300, F_start, P0 − L)`. R 마크(이미 재생 중)는 `L = 0`. 값이 음수(P0 < L)이면 `leadInMs = 0`으로 두고 조각을 마크 뒤 `(L − P0)` ms에서 시작한다(P0 = 0이면 마크 +15ms). **곡 맨 앞에서 누른 스페이스에는 리드인이 없다.**
> - `songPositionMs = (P0 − L) − lead`. `max(0, …)`으로 좌표를 눕히지 않는다 — 눕히면 곡 처음 조각이 315ms 뒤 좌표로 저장된다.
> - 불변식 `songPositionMs + leadInMs + L == P0`은 **P0 ≥ L일 때만** 성립한다.
> - 끝 바이트 대기(500ms)가 상한을 넘겨 끝이 잘렸고 세션이 살아 있으면 **한 번 더 기다려 같은 자리에 다시 자른다.** 그래도 짧으면 저장 토스트에 「끝이 조금 잘렸을 수 있습니다」를 덧붙인다(`SlicedTake.truncated`).
> - 조각은 WAV를 쓴 직후가 아니라 **녹음 목록(recordings.json)에 등록된 뒤에** 세션의 저장 대기열·사이드카에서 뺀다(`confirmTakeSaved`). 먼저 빼면 그사이 앱이 끝났을 때 WAV는 있는데 목록에도 없고 복구도 안 된다. 등록 직후에 죽으면 다음 부팅에 「복구됨」 사본이 하나 더 생길 수 있다 — 조각을 잃는 것보다 낫다.
> - 표시용 위치는 `RecordingTake.displayPositionMs`(= songPositionMs + leadInMs) 하나로 통일한다. 저장 토스트·취소 토스트·녹음 목록이 같은 숫자(스페이스를 누른 자리)를 말한다.

### 3.5 교차 검증 (표시만)

조각 진행 중 프레임 줄이 올 때마다 `(precisePosition − 파일시각)` 표본을 모은다(v5.15.2의 `CaptureAnchorEstimator` 재사용, 최소값). 개루프 식(P0 − L)과 40ms 넘게 어긋나면 `timelineSuspect`로 표시하고 스낵으로 알린다. **값은 개루프 식을 쓴다**(PositionClock의 resync 잡음은 양방향이라 자동 채택은 위험).

### 3.6 감시·복구

- 멈춤 판정 = **프로세스 종료** 또는 **(RMS 공백 ≥ 600ms AND 파일 크기 2틱 연속 정체)**. OR로 묶으면 UI가 밀렸을 때 오탐한다.
- 조각 도중에 세션이 죽으면 자르지 않고: 끊긴 데까지 살려 저장 → 재생 정지 → 고정 해제 → 큰 경고.
- 사이드카 `<세션id>.json`: 장치·게인·시작 시각·열린 조각의 시작 파일ms·저장 대기 구간. 스페이스 핫패스에서는 **재생을 건 뒤** 비동기로 쓴다.
- 부팅 시 남은 세션: 사이드카에 미저장 구간이 있으면 잘라서 「복구됨」 테이크로 등록하고(열린 조각의 끝은 파일 길이), 그 뒤 세션을 지운다. 사이드카가 없으면 7일 지난 것만 지운다.
- 고아 ffmpeg를 PID로 죽이지 않는다 — 이 PC는 영상 파이프라인 ffmpeg가 상시 돈다. `-t` 자멸 + 종료 훅(1초 상한) + 부팅 복구로 막는다.

> **구현 확정 (3.6)** — 위의 「열린 조각의 끝은 파일 길이」는 **세션 프로세스가 죽은 경우에만** 맞다. 앱이 죽으면 고아 ffmpeg가 `-t`까지 계속 쓴다(실측 — 부모가 죽고 파이프가 끊겨도 끝까지 쓴다).
> - **소유 판정은 파일 성장이 아니라 잠금이다.** 복구는 `<세션id>.lock`의 배타 잠금을 쥐어 본다. 못 쥐면 다른 인스턴스의 살아 있는 세션 — 건드리지 않는다. 쥐어지면 소유 앱이 죽은 것 — 고아가 쓰는 중이어도 **곧바로** 복구한다(복구는 부팅 때 한 번뿐이라, 건너뛰면 최대 45분 뒤의 다음 부팅으로 밀린다). 잠금 파일이 없는 세션만 예전처럼 「상한 안에 시작 + 250ms 사이에 자람」으로 짐작한다(폴백).
> - **열린 조각의 끝은 `min(파일 길이, 마지막 생존 표시 + 2.5초)`로 닫는다.** 조각이 열려 있는 동안 감시 타이머가 2초마다 사이드카에 `aliveFileMs`를 남긴다(파일 IO뿐 — 화면 알림 없음, 대기 중에는 쓰지 않는다). 마크 직후의 사이드카에도 실린다.
> - 조각은 **목록에 등록된 뒤에만** 「살렸다」로 친다(`onSlice`). 전부 등록된 세션만 지운다. 일부가 실패하면 세션을 두고 실패한 구간만 사이드카에 남겨(열린 조각 표시는 비움) 다음 부팅에 다시 한다.
> - 고아가 PCM을 쥐고 있어 못 지우면(ffmpeg는 공유 삭제 없이 연다) 사이드카를 「복구 끝(미저장 없음)」으로 다시 써 둔다 — 다음 부팅이 중복 등록 없이 치운다. 사이드카만 지우면 단서 없는 PCM(최대 259MB)이 7일 남는다.
> - 종료 훅은 저장 줄을 최대 3초 기다린다(긴 조각은 자르고 등록하는 데 0.3초를 넘긴다). 못 끝내도 마크가 사이드카에 남는다.

### 3.7 화면

- 조작판 고정 글자(기존 `ExcludeSemantics` Text)의 **문자열만** 바꾼다: 「● 고정 — 마이크 여는 중」 → 「● 고정 ON · 마이크 열림 · 입력 좋음/작음/없음」 → 「● 고정 — 마이크 끊김」. 노드를 새로 만들거나 지우지 않는다(2026-09-22 접근성 브리지 크래시 규칙 — `lib/widgets/center_alert.dart` 머리말).
- 상태는 버튼 `semanticsLabel`에도 덧붙인다.
- **고정 대기 중(조각 없음)에는 주기 알림을 하지 않는다.** 레벨 문구의 버킷(좋음/작음/없음)이 바뀔 때만 알린다 — 전체 화면 재빌드가 크래시 노출을 키운다.
- 고정 모드에서는 「녹음을 시작했습니다」 스낵을 띄우지 않는다. 저장 스낵: 「조각 저장 — 1:23부터 2.4초」.
- 고정 켤 때 900ms 입력 점검은 세션의 RMS 줄로 한다(별도 프로브 없음). 무음이면 CenterAlert + 고정 해제. **점검이 끝나기 전에는 스페이스를 거절**한다.
- Ctrl+R: 맨 앞에서 `_armedSaveChain`을 기다린 뒤 직전 조각을 물린다. 조각이 열려 있으면 「스페이스로 멈춘 뒤에 취소해 주세요」.
- R(고정 중): 재생을 건드리지 않고 같은 세션에 마크만 찍는다(`playbackAlreadyRunning: playing`).
- 장치·게인 변경(고정 중): 열린 조각 저장 → 세션 재기동 → 입력 점검 재실행.

> **구현 확정 (3.7)**
> - 「● 고정 — 마이크 끊김」은 **도중 끊김 경로에서는 그려지지 않는다.** 끊김 처리가 같은 동기 스택에서 고정을 풀어 글자 자체가 사라지고, 알림은 CenterAlert가 맡는다. 이 문자열은 열기 실패 직후 세션을 닫는 짧은 틈에만 보일 수 있다.
> - 끊김 경고의 제목은 사유에 중립이다(「녹음 고정이 꺼졌습니다」) — 상한 도달·재기동 무음도 이 길을 탄다. 조각 줄은 완료형이 아니다(「저장하는 중입니다」) — 저장은 경고 뒤에 돈다. 저장까지 실패하면 그 경고가 끊김 사유와 「고정이 꺼졌다」를 다시 담는다(큰 경고는 한 장뿐이다).
> - Ctrl+R: 직전 시도가 목록에 못 올라갔으면(0.5초 미만·저장 실패) **한 번 막는다**(「…취소할 것이 없습니다」). 안 막으면 목록의 맨 앞 = 그 앞의 멀쩡한 조각이 물리고 6초 뒤 파일까지 지워진다. 한 번 더 누르면 앞 조각을 물린다.
> - 끄는 안내(「녹음 고정을 껐습니다」)는 저장을 기다리기 **전에** 띄운다 — 뒤에 띄우면 마지막 조각의 저장 결과·무음 경고를 덮는다.
> - 고정 스페이스에서 재생이 막혀 조각을 취소했으면 「녹음도 시작하지 않았습니다 — 반주 없이 받으려면 R」을 알린다.
> - 장치·게인 변경은 **예약된 순간부터(디바운스 500ms 포함)** 재기동이 끝날 때까지 스페이스·R을 「마이크를 여는 중」으로 거절한다.

## 4. 함께 고치는 것

| # | 결함 | 위치 |
| --- | --- | --- |
| 1 | `RecordingStore.save()`가 비원자 writeAsString + 예외 삼킴. unawaited 반주 컷의 update()와 add()가 겹치면 JSON이 깨지고 load()가 []를 돌려줘 **목록 전체가 증발**한다 | recording_library_service.dart — 직렬 큐 + `.tmp`→rename + `.bak` |
| 2 | `_stitchTakes`의 lineStarts가 LRC 원본축(`l.time + timed.offsetMs`)이고 songPositionMs는 플레이어축 — `lyricsOffsetMs=1800`인 곡에서 줄 경계 스냅(±200ms)이 엉뚱한 곳을 잡는다 | song_list_screen.dart `_stitchTakes` — `LyricsSyncMath`의 플레이어축 환산으로 통일 |
| 3 | 이어붙이기가 무음 테이크를 포함하고, 스냅이 테이크 자기 시작보다 앞으로 가 음수 오프셋이 생긴다 | take_stitch_service.dart |
| 4 | 이어붙이기 내용 시작 판정이 리드인 안의 키 소음에 걸린다 | `StitchSegment.leadInMs` + silencedetect를 leadIn+250ms부터 |

> **구현 확정 (#1)** — 「지금 못 연」 정본(다른 프로세스의 잠금·오프라인 자리표시자)은 깨진 파일과 다르게 다룬다. 내용을 본 적이 없어 백업도 `.corrupt` 사본도 뜰 수 없으므로 **목록이 있어도 덮지 않는다**(저장 실패로 올린다). 그렇게 빈 목록·낡은 `.bak`으로 시작한 메모리 목록은 정본의 후손이 아니므로, 다음 저장 **전에** 정본을 다시 읽어 id로 합친다(같은 id는 메모리 쪽, 그동안 지운 id는 되살리지 않는다). 안 합치면 저장 두 번에 정본과 `.bak`이 모두 새 조각 하나짜리 목록으로 덮인다.

## 5. 완료 조건 (기계 판정)

- [ ] `flutter analyze` 이슈 0
- [ ] `flutter test` 전부 통과(기존 1046 + 신규)
- [ ] 순수 함수 테스트: `buildSessionCaptureArgs`, `CaptureClock`(지터·드리프트·갭·일괄 도착), `computeTakeSlice`(불변식·클램프·최소 길이), WAV 헤더·가장자리 가공, `writeTakeFromSession`(덜 자란 파일 대기·상한)
- [ ] 가짜 러너 컨트롤러 테스트: opening→live, 잠금 전 마크 거절, 마크 사이 `isTakeOpen`, 세션 즉사, 상한 도달 재기동
- [ ] 실기 테스트(`SP_REAL_FFMPEG=1`, RØDE): 세션 live ≤ 900ms, 임의 마크 5회의 조각 길이 오차 ≤ 1프레임(50ms) + 트림 상수, 종료 뒤 세션 폴더에 .pcm 0개·잔류 ffmpeg 0개
- [ ] `RecordingStore` 동시 쓰기 테스트: add/update 50회 교차 후 JSON 유효·항목 수 일치
- [ ] `lyricsOffsetMs=1800` 회귀 테스트: 줄 경계 스냅이 플레이어축에서 맞는다
- [ ] `grep`: `includeRepeats: false`(Space), `direct=1` 1곳, 세션 인자에 `-flush_packets`
- [ ] AppVersion 5.16.0 == pubspec, 빌드 산출물에 세션 코드 포함

## 6. 사용자 확인 필요 (위 목록과 분리)

- 실제 헤드폰으로 스페이스 ↔ 음악 체감, 첫 음절이 담기는지
- 시작·정지 키 클릭음이 조각에 남는지(상수: 가드 −40/+60ms, 끝 트림 60ms)
- 이어붙인 결과의 박 맞음
- 고정 중 Windows 마이크 사용 표시가 계속 켜져 있는 것에 대한 수용 여부

## 7. 구현 제약 (에이전트 공통)

- GUI 앱 실행 금지, 데스크톱 스크린샷 금지, `build_deploy.ps1` 실행 금지(실행 중인 앱을 강제 종료한다), git commit/push 금지.
- `testWidgets` 안에서 실제 비동기(프로세스 스트림)를 기다리면 가짜 시계에서 영영 안 끝난다 — 컨트롤러 테스트는 plain `test()`.
- 떴다 사라지는 UI에 시맨틱스 노드를 만들지 않는다. 상시 노드는 두고 라벨만 바꾼다.
- 파이썬은 `py -3.13`. Bash 히어독은 이중 백슬래시를 하나로 접는다 — 백슬래시가 든 내용은 Write/Edit 도구로.
- 콘솔은 cp949 — 한글 출력은 UTF-8 파일로 쓰고 읽는다.
- 주석은 한국어로 WHY를 적는다(이 저장소 관례). 파일 첫 줄 `// file: <경로>`.
- 실기 테스트는 장치를 이름으로 고른다 — 자동 선택은 꺼진 Razer 동글(디지털 무음)을 먼저 잡는다. `RØDE`가 든 장치를 우선한다.
